import { z } from 'zod';
// import the REAL project schemas + validation util via tsx? Use built? Let's replicate validateApiResponse exactly.
import { RangeSchema } from './src/types/domain/poker.ts';
